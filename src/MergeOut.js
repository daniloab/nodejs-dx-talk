import React from "react";
import styled from "styled-components";

export const Img = styled.img``;

const MergeOut = () => {
    return (
        <>
            <h1>Merge and Out Comparison</h1>
            <Img src="./img/merge-out-comparison.png" />
        </>
    );
};

export default MergeOut;
