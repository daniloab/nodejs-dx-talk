import { dark } from 'mdx-deck/themes';

export default {
  ...dark,
  colors: {
    ...dark.colors,
    background: '#04010F',
  },
  font: "'Nunito', sans-serif",
  monospace: "'Cascadia Code', monospace",
};
